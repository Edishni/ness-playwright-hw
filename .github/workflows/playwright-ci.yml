name: Playwright Tests CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    timeout-minutes: 120
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.44.0-jammy
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run Playwright Tests
      run: npx playwright test --project=${{ matrix.browser }} eBay-workflows.spec.ts
      env:
        CI: true
        HOME: /root
        # If you have Moon or Grid, you can set them here:
        # PLAYWRIGHT_WS: ws://moon.company.com/playwright/${{ matrix.browser }}/playwright-1.44.0
        # GRID_URL: http://selenium-hub:4444/wd/hub


    # Upload Playwright HTML report
    - name: Upload Playwright Report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: playwright-report-${{ matrix.browser }}
        path: playwright-report/
        retention-days: 7

    # Upload JUnit/XML results
    - name: Upload JUnit Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: junit-results-${{ matrix.browser }}
        path: test-results/
        retention-days: 7

    # Upload Allure raw results
    - name: Upload Allure Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: allure-results-${{ matrix.browser }}
        path: allure-results/
        retention-days: 7

  report:
    needs: test
    runs-on: ubuntu-latest
    if: always()

    steps:
    - uses: actions/checkout@v4

    # Upgrade Node to 20 so allure-commandline v3 runs cleanly
    - uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install Allure CLI (v2.22.0, Node 18 supported)
      run: npm install -g allure-commandline@2.22.0

    - name: Download all artifacts
      uses: actions/download-artifact@v4

    - name: Generate Allure HTML Report
      run: |
        results=$(ls -d allure-results-*)
        echo "Merging results from: $results"
        allure generate $results --clean -o allure-report

    - name: Upload Allure HTML Report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: allure-report
        path: allure-report/
        retention-days: 7

    - name: Generate Summary
      if: always()
      run: |
        echo "## Test Results Summary" >> $GITHUB_STEP_SUMMARY
        echo "| Browser | Status |" >> $GITHUB_STEP_SUMMARY
        echo "|---------|--------|" >> $GITHUB_STEP_SUMMARY
        for browser in chromium firefox webkit; do
          if [ -d "junit-results-$browser" ]; then
            echo "| $browser | ✅ Completed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| $browser | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
        done